<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Real V3 two-leaf asset, uniformly scaled to 78dp and centered so the
         artwork stays inside the 66dp safe circle of every launcher mask. -->
    <item
        android:width="78dp"
        android:height="78dp"
        android:gravity="center"
        android:drawable="@drawable/v3_logo" />
</layer-list>
