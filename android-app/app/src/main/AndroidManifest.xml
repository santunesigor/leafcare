<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />
    <application
        android:name=".LeafCareApplication"
        android:allowBackup="false"
        android:label="LeafCare"
        android:icon="@drawable/ic_leaf"
        android:supportsRtl="true"
        android:theme="@style/Theme.LeafCare"
        android:usesCleartextTraffic="false">
        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Password-recovery return link (declarative step only).
                 Requires the Supabase dashboard redirect allowlist to include
                 leafcare://auth/reset-password. Exchanging the recovery token
                 for a session is pending a real-backend round trip. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="leafcare" android:host="auth" android:path="/reset-password" />
            </intent-filter>
        </activity>
    </application>
</manifest>
